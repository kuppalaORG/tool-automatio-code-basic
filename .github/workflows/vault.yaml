name: Run vault setup
on: [workflow_dispatch]
jobs:
  Provision:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - name: Run Your vault
        run: ansible-playbook -i vault-internal.codedeploywithbharath.tech, -e ansible_user=ec2-user -e ansible_password=DevOps321 -e tool_name=vault tool-setup.yml
